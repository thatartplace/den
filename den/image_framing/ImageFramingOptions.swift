//

import UIKit

enum ImageFramingBackground {
    case borderColorMode
    case color(_: UIColor)
}

enum ImageFramingPosition {
    case center
    case top
    case left
    case right
    case bottom
}
